<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Cone Health | Agent Verification Script</title>
  <style>
    :root {
      --primary: #0b5cab;
      --primary-dark: #08457f;
      --secondary: #eaf4fb;
      --accent: #2f9fd8;
      --text: #17324d;
      --muted: #5f7488;
      --border: #d7e4ef;
      --success: #177245;
      --bg: #f7fbfe;
      --white: #ffffff;
      --shadow: 0 14px 36px rgba(14, 63, 110, 0.12);
      --radius: 18px;
    }

    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      font-family: Arial, Helvetica, sans-serif;
      background: linear-gradient(180deg, #eef6fc 0%, #f9fcfe 100%);
      color: var(--text);
    }

    .page {
      min-height: 100vh;
      padding: 32px 20px 48px;
    }

    .container {
      max-width: 980px;
      margin: 0 auto;
    }

    .topbar {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 16px;
      margin-bottom: 24px;
      flex-wrap: wrap;
    }

    .brand {
      display: flex;
      flex-direction: column;
      gap: 6px;
    }

    .brand-mark {
      display: inline-flex;
      align-items: center;
      gap: 12px;
      font-weight: 700;
      font-size: 28px;
      color: var(--primary-dark);
      letter-spacing: -0.02em;
    }

    .brand-icon {
      width: 44px;
      height: 44px;
      border-radius: 12px;
      background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%);
      display: inline-flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-size: 20px;
      box-shadow: 0 8px 20px rgba(11, 92, 171, 0.24);
    }

    .tagline {
      color: var(--muted);
      font-size: 14px;
    }

    .card {
      background: var(--white);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      box-shadow: var(--shadow);
      overflow: hidden;
    }

    .hero {
      padding: 28px 32px 20px;
      background: linear-gradient(135deg, rgba(11, 92, 171, 0.06), rgba(47, 159, 216, 0.1));
      border-bottom: 1px solid var(--border);
    }

    .hero h1 {
      margin: 0 0 8px;
      font-size: 30px;
      line-height: 1.15;
      color: var(--primary-dark);
    }

    .hero p {
      margin: 0;
      color: var(--muted);
      font-size: 15px;
      max-width: 720px;
    }

    form {
      padding: 28px 32px 32px;
    }

    .grid {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 20px;
    }

    .field,
    .field-full {
      display: flex;
      flex-direction: column;
      gap: 8px;
    }

    .field-full {
      grid-column: 1 / -1;
    }

    label {
      font-size: 14px;
      font-weight: 700;
      color: var(--text);
    }

    input,
    select,
    textarea {
      width: 100%;
      min-height: 48px;
      border: 1px solid var(--border);
      border-radius: 12px;
      padding: 12px 14px;
      font-size: 15px;
      color: var(--text);
      background: #fff;
      transition: border-color 0.2s ease, box-shadow 0.2s ease;
    }

    textarea {
      min-height: 88px;
      resize: vertical;
    }

    input:focus,
    select:focus,
    textarea:focus {
      outline: none;
      border-color: var(--primary);
      box-shadow: 0 0 0 4px rgba(11, 92, 171, 0.12);
    }

    .verification-box {
      grid-column: 1 / -1;
      border: 1px solid var(--border);
      border-radius: 14px;
      padding: 18px 18px 16px;
      background: var(--bg);
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 16px;
      flex-wrap: wrap;
    }

    .verification-copy {
      display: flex;
      flex-direction: column;
      gap: 4px;
    }

    .verification-copy strong {
      font-size: 15px;
    }

    .verification-copy span {
      font-size: 13px;
      color: var(--muted);
    }

    .toggle-wrap {
      display: inline-flex;
      align-items: center;
      gap: 12px;
      font-weight: 700;
      color: var(--primary-dark);
    }

    .toggle-wrap input[type="checkbox"] {
      width: 22px;
      height: 22px;
      min-height: auto;
      accent-color: var(--primary);
      cursor: pointer;
    }

    .status-pill {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      min-width: 74px;
      padding: 8px 12px;
      border-radius: 999px;
      background: #eef2f6;
      color: var(--muted);
      font-size: 13px;
      font-weight: 700;
    }

    .status-pill.true {
      background: rgba(23, 114, 69, 0.12);
      color: var(--success);
    }

    .actions {
      display: flex;
      gap: 12px;
      margin-top: 28px;
      flex-wrap: wrap;
    }

    .btn {
      border: 0;
      border-radius: 12px;
      padding: 13px 18px;
      font-size: 15px;
      font-weight: 700;
      cursor: pointer;
      transition: transform 0.15s ease, box-shadow 0.15s ease, opacity 0.15s ease;
    }

    .btn:hover {
      transform: translateY(-1px);
    }

    .btn-primary {
      background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%);
      color: white;
      box-shadow: 0 10px 22px rgba(11, 92, 171, 0.22);
    }

    .btn-secondary {
      background: white;
      color: var(--primary-dark);
      border: 1px solid var(--border);
    }

    .footer-note {
      margin-top: 16px;
      font-size: 12px;
      color: var(--muted);
    }

    @media (max-width: 760px) {
      .hero,
      form {
        padding: 22px 18px;
      }

      .grid {
        grid-template-columns: 1fr;
      }

      .field-full,
      .verification-box {
        grid-column: auto;
      }

      .hero h1 {
        font-size: 25px;
      }

      .brand-mark {
        font-size: 24px;
      }
    }
  </style>
</head>
<body>
  <div class="page">
    <div class="container">
      <div class="topbar">
        <div class="brand">
          <div class="brand-mark">
            <span class="brand-icon">+</span>
            <span>Cone Health</span>
          </div>
          <div class="tagline">We're right here with you</div>
        </div>
      </div>

      <div class="card">
        <div class="hero">
          <h1>Agent Verification Script</h1>
          <p>
            Use this page to capture patient and member details during the call.
            The layout is optimized for quick entry while keeping a clean, trusted healthcare look.
          </p>
        </div>

        <form>
          <div class="grid">
            <div class="field">
              <label for="firstName">First Name</label>
              <input type="text" id="firstName" name="firstName" placeholder="Enter first name" />
            </div>

            <div class="field">
              <label for="lastName">Last Name</label>
              <input type="text" id="lastName" name="lastName" placeholder="Enter last name" />
            </div>

            <div class="field">
              <label for="phone">Phone Number</label>
              <input type="tel" id="phone" name="phone" placeholder="(555) 555-5555" />
            </div>

            <div class="field">
              <label for="dob">Date of Birth</label>
              <input type="date" id="dob" name="dob" />
            </div>

            <div class="field">
              <label for="memberId">Member ID Number</label>
              <input type="text" id="memberId" name="memberId" placeholder="Enter member ID" />
            </div>

            <div class="field">
              <label for="preferredContact">Preferred Contact Method</label>
              <select id="preferredContact" name="preferredContact">
                <option value="">Select one</option>
                <option value="call">Call</option>
                <option value="email">Email</option>
                <option value="sms">SMS</option>
              </select>
            </div>

            <div class="field-full">
              <label for="address">Address</label>
              <textarea id="address" name="address" placeholder="Enter full address"></textarea>
            </div>

            <div class="verification-box">
              <div class="verification-copy">
                <strong>Identity Verification Status</strong>
                <span>Check the box when identity has been confirmed.</span>
              </div>

              <label class="toggle-wrap" for="verificationStatus">
                <input type="checkbox" id="verificationStatus" name="verificationStatus" />
                <span>Status:</span>
                <span id="statusText" class="status-pill">False</span>
              </label>
            </div>
          </div>

          <div class="actions">
            <button type="submit" class="btn btn-primary">Save Script Details</button>
            <button type="reset" class="btn btn-secondary" id="resetBtn">Clear Form</button>
          </div>

          <div class="footer-note">
            Internal use only. Confirm privacy and consent requirements before recording member information.
          </div>
        </form>
      </div>
    </div>
  </div>

  <script>
    const verificationCheckbox = document.getElementById('verificationStatus');
    const statusText = document.getElementById('statusText');
    const form = document.querySelector('form');
    const resetBtn = document.getElementById('resetBtn');

    function updateVerificationStatus() {
      const isVerified = verificationCheckbox.checked;
      statusText.textContent = isVerified ? 'True' : 'False';
      statusText.classList.toggle('true', isVerified);
    }

    verificationCheckbox.addEventListener('change', updateVerificationStatus);

    resetBtn.addEventListener('click', function () {
      window.setTimeout(updateVerificationStatus, 0);
    });

    form.addEventListener('submit', function (event) {
      event.preventDefault();
      alert('Agent script details captured. You can now wire this form into your Genesys Cloud workflow.');
    });

    updateVerificationStatus();
  </script>
</body>
</html>
